abbr -a -U -- - prevd
abbr -a -U -- = nextd

abbr -a -U gb git checkout -b 
abbr -a -U gc git checkout
abbr -a -U gf git fetch
abbr -a -U gr git rebase
abbr -a -U gri git rebase -i
abbr -a -U gp git push --force-with-lease
abbr -a -U gs git status
abbr -a -U gm git safe-rebase-target
